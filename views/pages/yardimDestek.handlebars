<section class="support-page">

  <h1>Yardım & Destek</h1>

  <p class="support-desc">
    MSFL Tarih Kulübü olarak; siteyle ilgili geri bildirimleri, içerik
    önerilerini ve iş birliği taleplerini önemsiyoruz. Aşağıdaki formu
    kullanarak bizimle kolayca iletişime geçebilirsin.
  </p>

  {{#if success}}
    <div class="alert success">{{success}}</div>
  {{/if}}

  {{#if error}}
    <div class="alert error">{{error}}</div>
  {{/if}}

  <form method="POST" action="/yardim-destek" class="support-form">
    <input type="hidden" name="_csrf" value="{{csrfToken}}" />

    <div class="form-group">
      <label for="name">Ad Soyad *</label>
      <input
        type="text"
        id="name"
        name="name"
        placeholder="Ad Soyad"
        required
      />
    </div>

    <div class="form-group">
      <label for="email">E-posta *</label>
      <input
        type="email"
        id="email"
        name="email"
        placeholder="ornek@mail.com"
        required
      />
    </div>

    <div class="form-group">
      <label for="topic">Konu</label>
      <select id="topic" name="topic">
        <option value="Teknik Hata">Teknik hata bildirimi</option>
        <option value="İçerik Önerisi">İçerik önerisi</option>
        <option value="İş Birliği">İş birliği / destek</option>
        <option value="Diğer">Diğer</option>
      </select>
    </div>

    <div class="form-group">
      <label for="message">Mesaj *</label>
      <textarea
        id="message"
        name="message"
        rows="6"
        placeholder="Mesajınızı buraya yazabilirsiniz..."
        required
      ></textarea>
    </div>

    <button type="submit">
      Gönder
    </button>
  </form>

</section>